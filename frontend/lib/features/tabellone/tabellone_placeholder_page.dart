import 'package:flutter/material.dart';

class TabellonePlaceholderPage extends StatelessWidget {
  const TabellonePlaceholderPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Tabellone placeholder')),
    );
  }
}
