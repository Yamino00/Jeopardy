import 'package:flutter/material.dart';

class GenerazionePlaceholderPage extends StatelessWidget {
  const GenerazionePlaceholderPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Generazione placeholder')),
    );
  }
}
