import 'package:flutter/material.dart';

class PartitaPlaceholderPage extends StatelessWidget {
  const PartitaPlaceholderPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Partita placeholder')),
    );
  }
}
